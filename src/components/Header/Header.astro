---
const currDate = new Date().toLocaleTimeString();
---

<header class="bg-primary-900 border-b-2 border-tertiary-base font-medium sticky top-0 z-10">
	<div class="container mx-auto px-4">
		<div class="flex items-center justify-between py-3 text-sm">
			<div class="flex items-center space-x-4">
				<span class="text-text-400 font-mono">atish@terminal: ~</span>
			</div>
			<div class="text-text-500 font-medium font-mono flex items-center space-x-4">
				<span class="hidden sm:inline">{currDate}</span>
				<span class="flex items-center space-x-1">
					<span class="w-2 h-2 bg-green-500 rounded-full animate-pulse"></span>
					<span>session: active</span>
				</span>
			</div>
		</div>
	</div>
</header>
