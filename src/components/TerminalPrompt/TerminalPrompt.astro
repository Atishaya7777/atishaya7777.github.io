---
interface Props {
	command: string;
	showCursor?: boolean;
	className?: string;
}

const { command, showCursor = false, className = "" } = Astro.props;
---

<div class={`text-tertiary-base mb-2 ${className}`}>
	<span class="text-secondary-400">atish@terminal</span>:<span class="text-tertiary-base">~</span>$ {command}
	{showCursor && <span class="animate-pulse ml-1">█</span>}
</div>
