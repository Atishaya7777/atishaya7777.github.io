---

import Layout from '../layouts/Layout.astro';

---


<Layout title="404 | Atish">
	<main class="container mx-auto px-6 md:px-12 lg:px-16 max-w-6xl">
		<section class="flex flex-col items-center justify-center min-h-[60vh] py-16 md:py-24 text-center">
			<div class="max-w-2xl mx-auto">
				<h1 class="text-6xl md:text-8xl font-bold text-primary-900 dark:text-primary-100 mb-8">
					404
				</h1>
				<h2 class="text-2xl md:text-3xl font-bold text-primary-900 dark:text-primary-100 mb-6">
					Page Not Found
				</h2>
				<p class="text-lg md:text-xl text-primary-700 dark:text-primary-200 font-light leading-relaxed mb-12">
					Hmm, looks like this page decided to take a walk. Even the most elegant proofs sometimes need a redirect, right?
				</p>
				
				<div class="flex flex-col sm:flex-row gap-4 justify-center">
					<button 
						id="btn:go-back"
						class="bg-secondary-600 dark:bg-secondary-500 text-white px-8 py-4 rounded-sm font-bold hover:bg-secondary-700 dark:hover:bg-secondary-600 transition-all duration-300 hover:shadow-lg hover:-translate-y-1"
					>
						Go Back
					</button>
					<button 
						id="btn:reload"
						class="bg-primary-100 dark:bg-primary-700 text-primary-900 dark:text-primary-100 border border-primary-200 dark:border-primary-600 px-8 py-4 rounded-sm font-bold hover:bg-primary-200 dark:hover:bg-primary-600 transition-all duration-300 hover:shadow-lg hover:-translate-y-1"
					>
						Reload Page
					</button>
				</div>

				<!-- Fun mathematical reference -->
				<div class="mt-16 bg-primary-50 dark:bg-primary-900 border border-primary-200 dark:border-primary-700 p-8 rounded-sm">
					<p class="text-base text-primary-800 dark:text-primary-200 font-medium italic">
						"In mathematics, you don't understand things. You just get used to them." 
						<br />
						<span class="text-sm text-primary-600 dark:text-primary-400">– John von Neumann</span>
					</p>
					<p class="text-sm text-primary-600 dark:text-primary-400 mt-4">
						Sometimes even the smartest algorithms lead us astray. Let's get back to familiar territory.
					</p>
				</div>
			</div>
		</section>
	</main>
</Layout>

<script>
	const goBackBtn = document.getElementById('btn:go-back');
	const reloadBtn = document.getElementById('btn:reload');

	goBackBtn?.addEventListener('click', () => {
		window.history.back();
	})

	reloadBtn?.addEventListener('click', () => {
		window.location.reload();
	})
</script>
